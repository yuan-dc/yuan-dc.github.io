{
  "name": "yuan-studio.github.io",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "css": "sass.cmd --watch src:public",
    "deploy": "sass.cmd src:public && cp src/index.html .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.64.1"
  }
}
